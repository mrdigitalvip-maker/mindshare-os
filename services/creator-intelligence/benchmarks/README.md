# Creator Intelligence benchmark ingestion

This directory intentionally contains no production dataset. A dataset is accepted only when every row passes `validateBenchmark`, including an HTTPS source reference, publication and collection dates, methodology, region, metric/window, limitations, and a version. Fixture data belongs only in tests. Global and personal observations are stored and presented separately.

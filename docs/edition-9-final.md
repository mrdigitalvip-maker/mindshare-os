# Edition 9 implementation phase closed

All planned Edition 9 source/contracts are now on the feature branch. The branch should move only through CI fixes/review fixes until merge. Deployment availability is not an implementation blocker and is handled by the automatic KIVRYN Deploy Queue after merge.
